{% extends 'baseAdmin.html.twig' %}

{% block title %}TypeComp{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Categorie</th>
                <td>{{ type_comp.categorie }}</td>
                {% if type_comp.categorie == "Compétence fonctionnelle" %}
                    <td></td>
                {% else %}
                    <td>
                        <a href="{{ path('type_comp_edit', {'id': type_comp.id}) }}">Edit name</a>
                        <p></p>
                        {{ include('type_comp/_delete_form.html.twig') }}
                    </td>
                {% endif %}
            </tr>
        </tbody>
    </table>
    <table class="table">
        <tbody>
        {% for competence in type_comp.competences %}
            <tr>
                <td>{{ competence.titre }}</td>
                <td>
                    <a href="{{ path('competence_edit', {'id': competence.id, 'idCategorie': type_comp.id }) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
            <tr>
                <td>
                    <a href="{{ path('competence_new', {'idCategorie': type_comp.id}) }}">Ajouter</a>
                </td>
                <td>

                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_comp_index') }}">Retour</a>
{% endblock %}
